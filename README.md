# Medirator Backend Repository (pruned)

This repository contains the backend FastAPI service in the `backend/` directory.

ML model loaders and training/demo scripts are kept in the `ML/` folder to avoid installing heavy ML dependencies in the backend deployment.

To deploy only the backend, cd into `backend/` and use the provided `requirements.txt` there.
